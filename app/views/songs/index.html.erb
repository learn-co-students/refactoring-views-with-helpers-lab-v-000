<ul>
<% @songs.each do |song| %>
  Artist: <li><%= display_artist(song) %> </li>
  Song: <li><%= link_to song.title, song_path(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
