<ul>
<% @songs.each do |song| %>
  <% # what was here before link_to song.title, song_path(song) %>
  <li><%= display_artist song %> – <%= link_to song.title, song_path(song) %></li>
  <% # so just added what i did before basically.  %>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
