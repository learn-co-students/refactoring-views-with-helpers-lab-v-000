<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <li><%= link_to song.artist_name, display_artist(song) %></li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
