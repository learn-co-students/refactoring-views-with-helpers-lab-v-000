<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <!-- Dry this up with helpers -->
  <ul>
    <%#if song.artist == nil%>
      <!-- <li><%#= link_to "Add Artist", edit_song_path(song) %></li> -->
    <%# end %>
    <li><%= display_artist song%></li>
  </ul>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
