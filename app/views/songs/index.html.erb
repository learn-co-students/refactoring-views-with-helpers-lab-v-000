<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <li>BY: <%= link_to song.artist.name, artist_path(song.artist) %> </li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
