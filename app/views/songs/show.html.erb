<h1><%= link_to @song.title, song_path(@song) %></h1>

  <%= display_artist @song %>
<% if @song.artist %>
  Artist: <%= link_to @song.artist.name, @song.artist %><br />
<% else %>
  Edit: <%= link_to "Add Artist", edit_song_path(@song) %>
<% end %>
<%= link_to "All Songs", songs_path %>
