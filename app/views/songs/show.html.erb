<h1>Song: <%= link_to @song.title, song_path(@song) %></h1>

<% if @song.artist_name.nil? %>
  <%= link_to "Add Artist", edit_song_path(@song) %>
<% else %>
  <h3>Artist: <%= link_to @song.artist_name, artist_path(@song.artist) %></h3>
<% end %>

<%= link_to "All Songs", songs_path %>
