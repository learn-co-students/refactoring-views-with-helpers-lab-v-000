<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.fields_for @artist.build do |artist_fields| %>
    <%= artist_fields.label :name %>
    <%= artist_fields.text_field :name %>
    <br>
  <%end%>
  <%= f.submit %>

<%end %>
